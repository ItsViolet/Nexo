{
    "name": "root",
    "private": true,
    "scripts": {
        "watch:tsc": "lerna exec -- \"npx tsc --watch\"",
        "watch:rollup": "lerna exec \"npx rollup -c --watch\"",
        "watch": "run-p watch:tsc watch:rollup",
        "postinstall": "lerna bootstrap"
    },
    "devDependencies": {
        "@types/luxon": "^2.0.9",
        "lerna": "^4.0.0",
        "npm-run-all": "^4.1.5"
    },
    "dependencies": {
        "@nexojs/command-line": "file:packages/command-line",
        "luxon": "^2.3.0",
        "typescript": "^4.5.5"
    }
}
